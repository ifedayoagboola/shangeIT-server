export type Rate = {
  // _id: string;
  name: string;
  alias: string;
  slug: string;
  image: string;
  currency: string;
  rate: number;
  amount: number;
  note: string;
  userRating: number;
};
